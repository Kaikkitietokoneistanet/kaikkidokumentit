/**
 * @name - document name
 * @content - document content
 * @owner - document owner
 * @collection - mongodb collection
 */
module.exports = newDocument = ({ name, content, owner, collection }) => {
    client
}